name: C/C++ CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: make
      run: make
      working-directory: ./src
    - name: make check
      run: make check
      working-directory: ./src
    - name: make distcheck
      run: make distcheck
      working-directory: ./src